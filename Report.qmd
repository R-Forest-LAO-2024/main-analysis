---
title: "Untitled"
format: 
  pdf:
    documentclass: scrartcl
    papersize: A4
editor: visual
---

## Session 04 : tree level calculations

example 01: join `tree_init` with `treeplot`:

```{r}
names(tree_init)
names(treeplot)

tt <- left_join(tree_init)
```

Exercise: join `tree_init` with `plot`

```{r}

```
